# Runtime dependencies for vibe-coding-evaluator
# Install with: pip install -r requirements.txt

# Core framework
litellm>=1.40.0
pydantic>=2.5
pyyaml>=6.0
python-dotenv>=1.0
tenacity>=8.2
rich>=13.7

# SAST tools (optional — install bandit and semgrep separately as CLI tools)
# See pyproject.toml [sast] extras

# LLM provider SDKs (optional — pick the ones you need)
openai>=1.30
anthropic>=0.20
mistralai>=0.4
ollama>=0.2

# Test infrastructure
pytest>=7.4
pytest-cov>=4.1
pytest-mock>=3.12
responses>=0.25

# Dashboard rendering
jinja2>=3.1
